# Implementation checkpoint log

User-requested file: `progess.md` (spelling retained). Started 2026-09-05.

## Scope and execution map

Implement the four September 5 plans: runtime (A), balanced Book budget (B), RAG/source fidelity (C), math/export fidelity (D). Latest budget spec wins: $0.05–$0.09 target, $0.10 ceiling per logical Book, including reasoning/retrieval/retries. Record changes, exact tests, outcomes, review and provider cost at every checkpoint. No commit/push or production cutover requested.

| Checkpoint | Plan tasks | Outcome | State |
| --- | --- | --- | --- |
| CP0 | Setup | Verify worktree, baseline, dependency/conflict review | Complete |
| CP1 | A1 | Atomic artifact/job progress | Complete |
| CP2 | A2 | Durable frontend job recovery/live polling | Complete |
| CP3 | A3 + B1 | Shared provider usage ledger and durable budget | Complete |
| CP4 | C1 | Source equations/tables and extraction coverage | Complete |
| CP5 | C2 | Real embedding identity, batching/cache | Complete |
| CP6 | C3 | Honest quality/retrieval evaluation | Complete |
| CP7 | C4 | Shared source plan across artifacts | Complete |
| CP8 | B2 + B3 | Budgeted candidate model/content allocation | Complete |
| CP9 | A4 + B4 | Resumable chapters, bounded deadlines and reuse | Complete |
| CP10 | A5 | Fair bounded chapter concurrency | Complete |
| CP11 | D1 | Preserve canonical math and narration separation | Complete |
| CP12 | D2 | Bundled rich text/math renderer | Review pending |
| CP13 | D3 | All-artifact renderer integration | Pending |
| CP14 | D4 | Validate exports before publication | Pending |
| CP15 | A6 | Realistic fake-provider 50/100-user load gate | Pending |
| CP16 | B5 + final gates | Capped real evaluation, quality/cost, broad review | Pending |

## CP0 — workspace and baseline

- Existing linked worktree verified: `D:/HackaGen-appearance-worktree`, branch `codex/distinctive-product-appearance`, base `fbac4134d4dac7bce417d4016c377ffe830631e7`; not a submodule. Existing untracked audit/plans preserved.
- Test: from backend, fake `OPENROUTER_API_KEY`, in-memory `DATABASE_URL`, `.venv/Scripts/python.exe -m pytest tests/test_job_tasks.py tests/test_job_idempotency.py -q`.
- First sandbox attempt: 21 passed, 25 setup errors due to denied Windows pytest temp-directory access; no product failure inferred. Escalated same-command rerun: **46 passed**, 326 pre-existing datetime/Starlette deprecation warnings, 4.13s test time.
- Estimated OpenRouter cost **$0.00**; actual **$0.00** (fake key, no model generation).
- Frontend baseline: `npm test -- src/hooks/usePollingArtifact.test.tsx src/components/dashboard/JobProgress.test.tsx src/components/dashboard/ArtifactJobRetry.test.tsx`: **24 passed**, 3 files, 5.01s. Estimated/actual OpenRouter cost **$0.00**.
- Starting billing snapshot at **08:10:07 UTC / 15:10:07 Vietnam**: key remaining **$9.02170362**, key usage **$0.97829638**, wallet remaining **$520.088690501**. Read-only lookup cost **$0.00**; key remains private.
- Baseline warnings remain visible; dependency-wide warning cleanup is not evidence of failure in this checkpoint.
- Skill workspace script needed escalation because Windows denied Bash signal-pipe creation; successful rerun created only worktree-local ignored review artifacts.

## Preflight rulings

- Ruling: retain this linked worktree and current feature branch — already isolated; no additional checkout needed — wrong choice would require moving changes later.
- Ruling: retain `progess.md` and review ledgers, use uncommitted task diffs instead of skill-prescribed commits/deletion — explicit user checkpoint record and repository no-commit instruction take precedence — costs extra review-artifact storage, no loss of audit evidence.
- Ruling: execute one implementation checkpoint at a time, with fresh implementer and separate reviewer — shared generator/LLM/metadata files overlap heavily and the selected skill requires sequential implementers — costs less parallel throughput, avoids conflicting edits.
- Ruling: merge overlapping A3/B1 and A4/B4 deliverables into single checkpoints — they describe the same usage/checkpoint storage interfaces — risk is a larger review surface, mitigated by focused tests and review.
- Ruling: latest Book budget limits retries to at most one only when reserved funds fit; automatic retries reuse the same logical budget — spending ceiling overrides earlier wording implying unconditional retry — some requests may stop safely rather than complete.
- Ruling: paid Book evaluation is limited to the planned three cases/$0.30 aggregate, each at most $0.10, and only after durable guards pass; load tests always fake — executing the plan authorizes its bounded smoke, not arbitrary spend — insufficient source/baseline may prevent promotion, which must be reported.
- Ruling: runtime changes authorized now; deployment/production data migration is prepared and tested on disposable state, not cut over automatically — execution request supersedes planning-only wording but does not request a production switch — deployment may remain a separate action.
- Ruling: plan code snippets are design examples, not permission to weaken tests or ignore transactional defects — implement equivalent minimal production-safe behavior — costs documented departures when examples are incomplete.

Detailed per-task and shared-interface preflight matrix is stored in each plan's scoped SDD ledger before its first dispatch.

## CP1 — atomic artifact/job progress

- Implementation: fenced atomic job/artifact progress, course metadata locking and explicit retry reset; seven new contract tests.
- Tests: `uv run --no-sync pytest tests/test_artifact_progress_contract.py tests/test_job_tasks.py tests/test_job_idempotency.py -q`: **53 passed**; scoped Ruff and `git diff --check` passed. Estimated/actual provider cost **$0.00**.
- Independent review: changes required. Expired-lease reclaim starts a new attempt without resetting its progress; fix round 1 dispatched with regression coverage. Checkpoint remains open.
- Estimated provider cost: **$0.00**; real provider calls prohibited.
- Fix round 1: guarded claims now reset progress for every new attempt. Covering backend suites: **54 passed**, scoped Ruff clean, diff whitespace check passed. Estimated/actual provider cost **$0.00**.
- Independent scoped re-review: **spec PASS; quality PASS**; reclaim finding addressed. CP1 complete. SQLite concurrency is tested; PostgreSQL locking still requires disposable integration validation.

## CP2 — durable frontend observation

- State: implementation starting; checkpoint test and independent review gates required.
- Ruling: add separate optional owned active-job envelope when the selected version differs — one job_id cannot represent both — cost is additive response fields and compatibility tests.
- Estimated provider cost **$0.00**; all mocked API and local browser checks must avoid paid calls.
- Initial validation: focused Vitest **26 passed**; ownership API tests **2 passed**; isolated generation status test **1 passed**; existing mocked Video browser scenarios **3 passed**; Next build passed; ESLint zero errors/three existing image warnings; scoped Ruff and diff check passed. Every listed run estimated/actual provider cost **$0.00**.
- Combined backend route/generation run: **32 passed, 2 failed** (`No vector chunks found`). Isolation diagnosis remains open; not accepted as a checkpoint pass.
- Implementer returned with missing explicit all-four-tab browser recovery scenarios. Returned to implementer to complete original scope and resolve/classify both failures before independent review; CP2 remains open.
- Follow-up diagnosis: stale vector/generator singleton state caused the two backend failures. Test fixture now uses unique Chroma collections and clears cached service handles. Generation suite: **10 passed**; combined original route/generation order plus CP1 contracts/tasks/idempotency: **88 passed**.
- Full Playwright matrix: **8 passed** in 2.0 minutes across all four artifacts, including old-version viewing, switching, remount, offline/online recovery, simulated delay beyond six minutes, terminal completion within five seconds, and late response after course navigation. An initial hidden-tab locator defect was corrected before the clean rerun; no product failure hidden.
- All follow-up runs estimated/actual provider cost **$0.00**. Independent spec/quality review dispatched; checkpoint remains open pending verdict.
- Independent review: **spec FAIL; quality FAIL**. Five findings: first-read transient failure stops discovery; cached-version race displays wrong content; reconnect abort races schedule overlapping reads and revive terminal observation; legacy polling retries 401/403; timezone-free retry timestamp is misread in Asia/Saigon. Reviewer reproduced timestamp serialization/parsing locally ($0).
- Fix round 1 dispatched with focused regressions and browser rerun. Also require active-job version membership validation to resolve the review's ownership/version-check uncertainty. CP2 remains open; prior passing tests did not cover these paths.
- Fix round 1 validation: **31 Vitest passed**, **24 backend envelope/route tests passed**, **8 Playwright passed**, Next build passed, scoped Ruff/diff check passed, ESLint zero errors/three existing image warnings. Estimated/actual cost of every run **$0.00**. Separate preview/poll request fencing, explicit terminal/session stops, UTC timestamps and active version membership checks implemented.
- Scoped re-review dispatched. Report transparently records that new tests were not run before the fix; failing old expectation/test cleanup runs are not presented as genuine pre-fix regression evidence.
- Fix round 1 review: selected-version race, auth handling, UTC serialization, and active version membership **addressed**. Initial recovery still retains stale error text; cancellation bypasses terminal polling stop. Spec/quality remain **FAIL**; fix round 2 dispatched for these two residuals with focused red/green tests.
- Fix round 2: three new regressions run before product edits failed as expected (**3 failed,22 passed**). After stale-error clearing and terminal cancellation fencing: **34 Vitest passed**; lint zero errors/three existing image warnings; diff check passed. Sandbox `spawn EPERM` was resolved by rerunning with escalation. All runs estimated/actual provider cost **$0.00**. Scoped review pending.
- Fix round 2 independent re-review: **spec PASS; quality PASS**. Both residual findings addressed; no new fix-scope breakage. CP2 complete. Broader backend/browser/build evidence and focused post-fix tests are recorded above; remaining minor coverage notes are in the scoped ledger for final review.

## CP3 — provider accounting and durable Book budget

- State: implementing combined A3/B1 through fresh architecture implementer; checkpoint tests and independent review required.
- One version-owned budget survives retries; exact monetary reservations precede provider calls; response usage persists before content/schema parsing. Unknown charges must remain reserved and prevent further paid work.
- Estimated provider cost **$0.00**. Only fake usage/provider tests and disposable migration checks authorized in this checkpoint; real smoke waits for later quality and budget gates.
- Interim implementer evidence: **129 targeted tests passed**, OpenRouter cost **$0.00**; independent review has not yet run. Pinned offline tokenizers now measure Gemini requests and OpenAI embedding inputs without runtime downloads.
- Ruling: retain measured full-request token counts plus a documented conservative framing allowance and explicit calibration state, rather than reserving the model's entire context window for every request — OpenRouter's hidden framing maximum is not published, and a whole-window reservation would prevent every affordable Book — an underestimated allowance remains a risk to be checked by capped calibration and provider usage reconciliation before promotion. Do not label an uncalibrated estimate a proved maximum. Exact ledger admission, provider price caps, unknown-charge holds and over-reservation incident handling remain mandatory.
- Implementation frozen; independent spec/quality review dispatched. Final accounting tests **28 passed**, corrected queue tests **22 passed**, Ruff clean. SQLite and disposable PostgreSQL16 upgrade/downgrade/re-upgrade, eight concurrent reservations and duplicate settlements passed. All estimated/actual provider costs **$0.00**.
- Broader historical run: **400 passed,5 skipped,4 failed** from queue payload assertions after internal budget identity was added. Follow-up test edits initially yielded **114 passed,2 failed** from misplaced assertions; corrected focused queue suite above passed. These historical full runs are not reported as clean. No production container/database migration occurred.
- CP3 leaves actual Gemini paid admission behind an explicit uncalibrated-framing gate, with concrete offline request estimates available to CP8. Calibration and the complete source-dependent paid smoke allowance belong to CP16; no paid readiness/promotion is claimed here.
- Independent review found two important concurrency defects ($0 focused reproductions): stale unknown-charge settlement can overwrite an overcharge incident and later reopen admission; final dispatch can send an already reserved call after its budget becomes blocked. CP3 remains open; original implementer will receive scoped fix round 1 with red/green regression requirements.
- Fix round 1 dispatched for both findings. Ruling: the atomic, budget/ownership-fenced `reserved -> dispatched` transition is the dispatch authorization point — a database transaction cannot undo an external request already authorized and sent — incidents stop subsequent dispatch authorization; no long database lock may be held across a provider request. Tests must distinguish an incident before this transition from an incident after it.
- Fix round 1 interim red/green: **9 failed,1 passed** before product edits (2.96s), then **10 passed** (2.40s). Covers stale unknown/incident reconciliation, seven reserve-to-dispatch interruption cases through the real fake embedding boundary, and an incident-after-dispatch control. Estimated/actual provider cost **$0.00**. Broader accounting and PostgreSQL interleaving checks still underway; review gate remains open.
- Fix round 1 frozen: **99 accounting/provider/parsing tests passed** (9.83s), **11 targeted PostgreSQL16 interleaving/concurrency tests passed** (4.79s), Ruff clean. Only provider_usage.py and its tests changed. Estimated/actual provider cost **$0.00**; disposable resources removed. Scoped independent re-review dispatched.
- Fix round 1 independent re-review: **spec PASS; quality PASS**. I1/I2 addressed, no new fix-scope breakage or out-of-scope findings. CP3 complete with the explicit downstream paid-calibration/promotion limits retained.

## CP4 — canonical source extraction and coverage

- State: fresh source-extraction implementer dispatched. Preserve DOCX body/table/OMML ordering, per-page PDF damage/OCR coverage and strict UTF-8. Actual extraction seams and legacy unknown-coverage ruling are in the task brief.
- Estimated provider cost **$0.00**; all tests use valid synthetic fixtures and injected OCR/fake credentials. No live document/index changes or paid reindexing. Checkpoint acceptance requires red/green source/OCR tests, side-by-side fixture inspection and independent review.
- Interim root causes confirmed: DOCX paragraph-only traversal drops tables/Office Math and body ordering; PDF whole-document scan-majority/sampling misses mixed scans; PDF/DOCX binary decoding fallbacks and TXT ignore-errors lose data; OCR string-only return hides truncation.
- RED before product edits: source-fidelity suite collection failed for missing docx_extract module (after resolving sandbox UV-cache denial). Initial GREEN: **9 source-fidelity tests passed**; combined source/document/OCR/provider suites **78 passed**, scoped Ruff clean. All runs estimated/actual provider cost **$0.00**. Broader regression and side-by-side fixture inspection still in progress; checkpoint remains open.
- Implementation frozen and independent review dispatched: final **10 source tests passed**, **79 source/document/OCR/provider tests passed**, **88 upload/retry/accounting tests passed,1 existing skip**; Ruff clean and Alembic single head e1f2a3b4c5d6. Included accounting migration test upgrades/downgrades/re-upgrades the current head on disposable SQLite. Every listed run estimated/actual provider cost **$0.00**.
- Fixture/output inspection confirms paragraph/table/nested content and canonical x+y=2 and fraction grouping survive. Mixed PDF OCR covers first12scan candidates; omitted page and truncated OCR are marked incomplete. Full-backend attempt returned no final summary and is **not** claimed passed. Independent review pending.
- Independent review confirmed important gaps with $0 offline probes: inline text/math/text DOCX fragments share a location and yield duplicate chunk IDs; compound superscript bases lose grouping while being marked complete; empty successful OCR can produce no extracted/skipped/damaged page yet complete=True. Front-matter metadata compatibility is also under review. Checkpoint remains open for scoped fixes.
- Independent verdict **spec FAIL; quality FAIL**, five Important findings. The three reproduced defects above plus removed front-matter/noise protection and a damaged-symbol test that mocks the assessor itself. Fix round1 dispatched to original implementer with red/green regressions for all5, meaningful short-math/table preservation, and related UTF-8 guidance/docstring cleanup. Reviewer probes cost **$0.00**. Existing dependency warnings and unrelated trailing-blank-line findings remain minor final-review debt.
- Additional $0 source audit: the existing 174-page PDF has 147,170 native characters; six pages have no text/images/drawings and render entirely white, while pages 134–136 each have 10 replacement/private-use characters. Implementer informed to distinguish actual blank pages from failed empty OCR. No source/index mutations or paid OCR; full evidence in the evaluation source inventory.
- Fix round 1 frozen: **16 source tests passed**, **86 source/document/OCR tests passed**, **88 upload/retry/accounting tests passed,1 existing skip**, **4 focused OCR failure tests passed**, Ruff clean. Estimated/actual provider cost of every run **$0.00**. Red run before product edits was a collection ImportError for the new real assessor interface; it is not claimed as five individual behavioral failures. Earlier reviewer probes supply concrete failures for the three reproduced runtime defects. Scoped re-review pending.
- Fix round 1 independent re-review: original five Important findings **addressed**, but checkpoint remains **FAIL** because inline BI/EI PDF images can render visibly while `get_images(full=True)` reports zero, causing false `blank_pages=[1]`, skipped OCR and `complete=True`. Focused source suite **16 passed**; inline-image probe reproduced the regression; retry variants hit socket-denial setup errors only. Estimated/actual provider cost **$0.00**. Fix round 2 snapshot: `.superpowers/sdd/2026-09-05-rag-and-source-fidelity/cp4-fix2-base.json`.
- Fix round 2: inline BI/EI image blocks now count as displayed image evidence before true-blank certification. RED before product edit: inline-image regression failed with OCR calls `[]` instead of `[1]`. GREEN: focused inline/blank distinction **2 passed**, full source fidelity **17 passed**, related vector/document/LLM OCR/provider contracts **70 passed**, Ruff clean. Estimated/actual provider cost **$0.00**.
- Fix round 2 independent re-review: **spec PASS; quality PASS**. Inline images now dispatch OCR and remain damaged/incomplete when OCR returns empty; true blank pages remain blank-complete; no original CP4 finding regressed; no new Important or Minor findings. Independent verification: source fidelity **17 passed**, mixed-PDF probe passed, Ruff clean. Estimated/actual provider cost **$0.00**. CP4 complete.

## CP5 — explicit embedding pipeline

- State: implementation frozen; independent review pending. Added explicit embedding cache/service, batch validation, CP3 accounting hooks, Chroma identity metadata, explicit vector upsert/query, HTTP adapter default `embedding_function=None`, config settings and lazy vector construction for artifact compatibility. Verification reported by implementer: RED missing `embedding_cache` module before product edit; focused embedding **23 passed**; CP5/vector/client/provider/source regressions **149 passed**; document/generation/artifact compatibility **122 passed,1 skipped,2 failed** with failures attributed to invalid pre-CP4 fake PDF fixtures; Ruff clean; diff check clean except unrelated existing load-smoke trailing blank. Estimated/actual provider cost **$0.00**. Review package: `.superpowers/sdd/2026-09-05-rag-and-source-fidelity/task-2-review-package.md`.
- Independent review: **spec FAIL; quality FAIL** for one Important finding. Empty existing Chroma collections with mismatched identity try to `modify()` metadata containing immutable `hnsw:space`, yielding a raw Chroma error instead of the required `CollectionIdentityError` migration message. Reviewer confirmed the two compatibility failures are inherited invalid PDF fixtures, not CP5 regressions, by replacing only dummy PDF bytes in memory and getting both tests to pass. Independent checks: **59 embedding/accounting tests passed**, **10 status/progress tests passed**, real Chroma identity probe reproduced the defect. Estimated/actual provider cost **$0.00**. Fix round 1 snapshot: `.superpowers/sdd/2026-09-05-rag-and-source-fidelity/cp5-fix1-base.json`.
- Fix round 1: removed empty-collection metadata adoption; missing/mismatched Chroma identity now always raises `CollectionIdentityError` without modifying metadata. RED: three real-Chroma identity cases raised Chroma immutable-distance `ValueError`; GREEN: those three passed; focused embedding/accounting/provider/vector-client suite **127 passed**; Ruff and scoped diff check clean. Estimated/actual provider cost **$0.00**.
- Fix round 1 independent re-review: **spec PASS; quality PASS**. Identity mismatch behavior fixed for empty, legacy and emptied-after-use collections; HTTP path preserves migration error; no new Important or Minor findings. Independent focused suite **127 passed**, Ruff clean. Estimated/actual provider cost **$0.00**. CP5 complete.

## CP6 — honest RAG and quality metrics

- State: implementation frozen, independent review failed; fix round 1 dispatched. Implementer reported honest `QualityReport`, citation validation, source-faithful retrieval/dedupe/noise abstention, extraction completeness, a 10-document/30-case corpus, offline evaluator and frontend quality labels. Focused backend **24 passed**, frontend **21 passed**, Ruff/ESLint clean, Next build passed, related suite **129 passed,3 failed**. Estimated/actual provider cost **$0.00**.
- Independent review: **spec FAIL; quality FAIL** with five Important findings: missing document/topic coverage before Book outlining, generated quality reports discarded at publication/read boundary, evaluator treats missing observations as abstentions, CP6 report-version drift misclassified as inherited fixture debt, and mixed-scan corpus member is only a transcript. Minor: same-page source-order gap and case source-hash/member validation. Independent checks: **67 backend passed,1 CP6 version failure**, **21 frontend passed**, two invalid-PDF failures confirmed inherited by in-memory valid-PDF substitution. Estimated/actual provider cost **$0.00**. Fix round 1 snapshot: `.superpowers/sdd/2026-09-05-rag-and-source-fidelity/cp6-fix1-base.json`.
- Fix round 1: implementer reports all five Important findings and both Minor findings fixed. GREEN: relevant backend **84 passed**, generation/version/read suite **30 passed,2 inherited invalid-PDF cases deselected**, evaluator **6 passed**, frontend **21 passed**, Ruff/ESLint clean, Next build passed. Estimated/actual provider cost **$0.00**.
- Fix round 1 independent re-review: **spec FAIL; quality FAIL**. Original findings 3,4,5 addressed and report persistence core addressed, but coverage acquisition still fetches only the first two rows per source before filtering front matter/noise; new Important lifecycle regression leaves deleted active-version quality reports in `StudyPack`; Minor canonical same-page ordering still lacks production `document_location` metadata. Independent focused backend **84 passed** plus probes reproduced both Important issues and the ordering gap. Estimated/actual provider cost **$0.00**. Fix round 2 snapshot: `.superpowers/sdd/2026-09-05-rag-and-source-fidelity/cp6-fix2-base.json`.
- Fix round 2: bounded usable-evidence pagination now scans past front matter/noise, StudyPack public quality reports derive from active version metadata, and actual ingestion stores canonical ordering metadata. RED before product edits: four targeted tests failed; GREEN: targeted **4 passed**, retrieval/source/evaluator/vector group **72 passed**, quality/generation/version group **45 passed,2 inherited invalid-PDF cases deselected**, Ruff and scoped diff check clean. Estimated/actual provider cost **$0.00**.
- Fix round 2 independent re-review: **spec PASS; quality PASS** with one nonblocking Minor. Independent focused suite **87 passed**, Ruff clean, bounded scanning/order/deletion probes passed. Public StudyPack responses are correct after version deletion; internal cached aggregate may remain stale but is not used by reviewed public readers, and the report wording was corrected. Estimated/actual provider cost **$0.00**. CP6 complete.

## CP7 — shared SourcePlan across artifacts

- State: implementation frozen; independent review failed; fix round 1 dispatched. Added private shared `SourcePlan` schema/service and wired Book, Slide, Quiz and Video to one plan revision/model/evidence set. Implementer reported focused **15 passed**, broad related **79 passed,2 inherited invalid-PDF tests deselected**, Ruff clean. Estimated/actual provider cost **$0.00**.
- Independent review: **spec FAIL; quality FAIL** with four Important findings: concurrent metadata binding can erase plan history; plan model may differ from selected artifact model; tests copy canonical equations/definitions into hidden provenance without checking visible generated content; Book unit evidence expands to neighboring overlapping-title units. Minor: blank/ambiguous objective schema validation. Focused tests **15 passed**, Ruff clean, deterministic probes reproduced all four findings; two invalid-PDF failures remain inherited. Estimated/actual provider cost **$0.00**. Fix round 1 snapshot: `.superpowers/sdd/2026-09-05-rag-and-source-fidelity/cp7-fix1-base.json`.
- Fix round 1: implementer moved source plans into an independent `source_plans` table, keyed plans by actual selected feature model, added exact unit evidence assignments, visible canonical insertion/rejection, and stricter schema validation. GREEN: focused **20 passed**, broad **90 passed,4 skipped,2 deselected**, relevant generation+CP6 **17 passed**, Ruff clean. Estimated/actual provider cost **$0.00**.
- Fix round 1 independent re-review: **spec FAIL; quality FAIL** for one remaining Important finding. Canonical consistency rejects simple all-wrong output, but still accepts visible contradictions when canonical text is also present; Book summary/top-level visible fields are not checked. Findings 1,2,4 and schema Minor addressed. Independent focused **73 passed,4 skipped**, Ruff clean; probes reproduced the blocker for all four artifact schemas. Estimated/actual provider cost **$0.00**. Fix round 2 snapshot: `.superpowers/sdd/2026-09-05-rag-and-source-fidelity/cp7-fix2-base.json`.
- Fix round 2: implementer reports mixed canonical/contradictory claims rejected across Book, Slide, Quiz and Video, Book summary/top-level visible fields checked, and wrong selected Quiz answers cannot be masked by a correct explanation. GREEN: focused **23 passed**, broad **93 passed,4 skipped,2 deselected**, Ruff/diff clean. Estimated/actual provider cost **$0.00**.
- Fix round 2 independent re-review: **spec FAIL; quality FAIL** for the same remaining canonical-consistency blocker. The matcher still accepts changed equation operators, negated definitions and conflicting named claims in one sentence when canonical text is also present; selected Quiz answers can still be wrong. Independent focused **70 passed**, Ruff clean, probes accepted 17 contradictory cases. Estimated/actual provider cost **$0.00**. Fix round 3 snapshot: `.superpowers/sdd/2026-09-05-rag-and-source-fidelity/cp7-fix3-base.json`.
- Fix round 3: implementer replaced lossy normalization with exact claim-value comparison preserving operators/grouping and independent repeated-claim extraction. GREEN: exact review regressions **8 passed**, SourcePlan+CP6 **31 passed**, relevant generation/source/vector **51 passed**, broad **101 passed,4 skipped,2 inherited invalid-PDF exclusions**, Ruff/diff clean. Estimated/actual provider cost **$0.00**.
- Fix round 3 independent re-review: **spec FAIL; quality FAIL** for mathematical punctuation. Factorial changes still pass because `!` is stripped/terminates claims; correct decimal equations such as `0.5` can be rejected; Minor repeated `and` claims without comma are rejected. Focused **78 passed**, Ruff clean; 33 probes exposed the issues. Estimated/actual provider cost **$0.00**. Fix round 4 snapshot with fresh stronger implementer required: `.superpowers/sdd/2026-09-05-rag-and-source-fidelity/cp7-fix4-base.json`.
- Fix round 4: fresh stronger implementer preserved factorial/decimal math tokens and repeated `and`/`but` claim splitting. RED: **39 failed,37 passed** across new math-claim cases before product edit; GREEN: new cases **76 passed**, broad relevant **177 passed,4 skipped,2 inherited invalid-PDF exclusions**, Ruff clean. Estimated/actual provider cost **$0.00**.
- Fix round 4 independent re-review: **spec PASS; quality PASS**. Factorials, decimals, sentence boundaries and repeated claims behave correctly; accepted CP7 behavior and CP6 `faithfulness=None` intact. Independent focused **154 passed**, **204 probes passed**, Ruff clean. Estimated/actual provider cost **$0.00**. CP7 complete.

## CP8 — budgeted candidate model and content allocation

- State: starting. Scope combines balanced budget tasks B2/B3: choose/use lower-cost model policy and content allocation so a logical Book targets **$0.05–$0.09** and never exceeds **$0.10** including retrieval/source-plan/reasoning/review/repair/retries. No paid calls in this checkpoint; CP16 owns capped real calibration.
- Implementation frozen; independent review failed; fix round 1 dispatched. Implementer reported immutable Flash policy, source-evidence chapter allocation, adaptive caps, targeted context and safe truncation/infeasible outcomes. GREEN: **210 relevant tests passed**, Ruff clean, Alembic single head `a3b4c5d6e7f8`, CP8-scoped diff clean; provider/network calls none. Estimated/actual provider cost **$0.00**.
- Independent review: **spec FAIL; quality FAIL** with five Important findings: source-plan calls bypass saved Book policy and can use Pro; request estimation overwrites saved provider routing such as throughput; allocator drops unassigned evidenced objectives; empty cited chapters pass completion and can score 85; chapter input bounds are disconnected from actual measured prompt/reservation size. Minor: old fixed chapter-count prompt instructions conflict with adaptive one-unit-per-plan-unit behavior. Focused **210 passed**, SQLite migration probe passed, deterministic probes reproduced findings. Estimated/actual provider cost **$0.00**. Fix round 1 snapshot: `.superpowers/sdd/2026-09-05-balanced-book-budget/cp8-fix1-base.json`.
- Fix round 1: implementer reports saved-policy source-plan binding, final routing preservation, orphan-objective rejection, stricter chapter completion, measured final-request bounds and prompt-count cleanup. GREEN: focused **20 passed**, relevant suite **217 passed**, Ruff clean, Alembic head `a3b4c5d6e7f8`. Estimated/actual provider cost **$0.00**.
- Fix round 1 independent re-review: **spec FAIL; quality FAIL**. Addressed: Book-specific saved policy, final routing preservation, orphan-objective rejection, prompt count cleanup. Remaining Important: chapter completion checks count but not assigned objective identities; complete-scope feasibility/rebudgeting is not maintained across source-plan work and settlements; new CP7 regression where non-Book artifacts reuse cached Book Flash plan instead of selected feature model. Independent **217 passed**, Ruff clean, probes reproduced gaps. Estimated/actual provider cost **$0.00**. Fix round 2 snapshot: `.superpowers/sdd/2026-09-05-balanced-book-budget/cp8-fix2-base.json`.
- Fix round 2: implementer reports exact assigned/delivered objective sets, measured source-plan request/retry/residual before creation, durable capacity rereads before/after outline and each remaining chapter, and restored non-Book selected-model SourcePlan identity. GREEN: focused **5 passed**, relevant suite **221 passed**, Ruff clean, Alembic head `a3b4c5d6e7f8`. Estimated/actual provider cost **$0.00**.
- Fix round 2 independent re-review: **spec FAIL; quality FAIL** for remaining Important I4. I5 and I6 addressed. Objective labels now match as a set, but they are not bound to instructional section content; casefolding changes math/programming identifier identity (`Y` vs `y`). Independent **221 passed**, Ruff/diff clean, probes reproduced the gap. Estimated/actual provider cost **$0.00**. Fix round 3 snapshot: `.superpowers/sdd/2026-09-05-balanced-book-budget/cp8-fix3-base.json`.
- Fix round 3: stable objective IDs and exact case-sensitive objective text now flow into chapter generation, with `objective_coverage` mapping assigned objectives to nonempty instructional sections. RED reproduced missing objective content, `Y`/`y` acceptance and case-distinct collapse; GREEN: focused **5 passed**, relevant suite **226 passed**, two legacy generation checks passed, Ruff/Alembic/diff clean. Estimated/actual provider cost **$0.00**.
- Fix round 3 independent re-review: **spec PASS; quality PASS**. I4 addressed; accepted I5/I6 and prior CP8 fixes remain intact. Independent focused **228 passed**, **12 probes passed**, Ruff/diff clean. Estimated/actual provider cost **$0.00**. CP8 complete.

## CP9 — resumable Book checkpoints and bounded deadlines

- State: implementation frozen; independent review failed; fix round 1 dispatched. Added private typed Book checkpoints, retry reuse, original budget/options/policy preservation, identical-ready cache before admission, explicit OpenRouter timeout settings, soft deadline handling and deletion cleanup. Implementer reported new checkpoint suite **9 passed**, focused/relevant **115 passed**, Ruff clean, Alembic unchanged, provider cost **$0.00**.
- Independent review: **spec FAIL; quality FAIL** with four Important findings: retry preflight still charges/gates already completed work; configured overall attempt timeout has no elapsed effect; LLM broad exception handler swallows `SoftTimeLimitExceeded` and retries dispatch; ready-cache lookup can return a concurrently deleted version. Minor: stale-attempt/lease and crash-window coverage incomplete. Focused **42 passed**, probes reproduced defects, invalid-PDF failures inherited. Estimated/actual provider cost **$0.00**. Fix round 1 snapshot: `.superpowers/sdd/2026-09-05-live-progress-and-capacity/cp9-fix1-base.json`.
- Fix round 1: implementer reports remaining-work capacity after checkpoint discovery, elapsed attempt deadline with unknown accounting, soft-limit propagation with one dispatch, final locked ready-cache revalidation, and expanded fence/commit-fault coverage. GREEN: focused **55 passed**, CP2/CP3/CP8 **123 passed**, Ruff/diff clean, Alembic unchanged. Estimated/actual provider cost **$0.00**.
- Fix round 1 independent re-review: **spec FAIL; quality FAIL** for one new Important race. Original four findings addressed for Book path, but failed checkpoint rollback releases DB locks before restoring backup, so it can overwrite a newer committed checkpoint. Remaining Minor: integrated SDK soft-limit to durable reschedule with retained chapters coverage missing. Independent **123 passed**, probes reproduced rollback defect and OCR soft-limit propagation. Estimated/actual provider cost **$0.00**. Fix round 2 snapshot: `.superpowers/sdd/2026-09-05-live-progress-and-capacity/cp9-fix2-base.json`.
- Fix round 2: implementer reports rollback recovery now reacquires J→C fence and restores only if target still matches failed writer bytes; integrated soft-limit durable retry with retained chapters added. GREEN: recovery **2 passed**, focused **56 passed**, CP2/CP3/CP8 **124 passed**, Ruff/Alembic/diff clean. Estimated/actual provider cost **$0.00**.
- Fix round 2 independent re-review: **spec FAIL; quality FAIL** for the same rollback race. Content hash equality is not write ownership; a newer valid writer committing identical deterministic bytes can still be restored over or unlinked. Integrated soft-limit coverage addressed. Independent **124 passed**, probes reproduced identical-byte overwrite/unlink. Estimated/actual provider cost **$0.00**. Fix round 3 snapshot: `.superpowers/sdd/2026-09-05-live-progress-and-capacity/cp9-fix3-base.json`.
- Fix round 3: rollback recovery now uses a unique per-save promotion token instead of content equality, so identical deterministic checkpoint bytes from a newer writer are not overwritten or unlinked by an older failed writer. RED: identical-byte rollback race probes reproduced the overwrite/unlink defect; GREEN: targeted recovery **5 passed**, focused checkpoint suite **58 passed**, CP2/CP3/CP8 related suites **126 passed**, Ruff/Alembic/diff clean. Estimated/actual provider cost **$0.00**.
- Fix round 3 independent re-review: **spec PASS; quality PASS**. Unique promotion tokens preserved newer identical-content writes in both prior/no-prior cases; no Important or Minor findings remain in CP9 scope. Independent evidence: **126 focused tests passed**, **4 deterministic probes passed**, Ruff clean. PostgreSQL contention/crash atomicity remains unexecuted and is retained for the final integration gate. Estimated/actual provider cost **$0.00**. CP9 complete.

## CP10 — fair bounded chapter concurrency

- State: starting. Scope is bounded per-Book chapter concurrency, shared fair provider admission across users/jobs, short capacity continuations that do not consume failure retries or provider budget, explicit provider context binding for chapter workers, and concise frontend stage labels. CP10 must preserve CP9 checkpoints/resume, CP8 allocation, CP7 SourcePlan identity, CP3 accounting and CP2 live progress. No real provider calls or paid load tests in this checkpoint; estimated/actual provider cost remains **$0.00** until a later capped gate.
- Implementation frozen for independent review. The first CP10 implementer was interrupted before a report; root completed the partial diff and wrote `.superpowers/sdd/2026-09-05-live-progress-and-capacity/task-5-report.md`.
- Implemented bounded Book chapter scheduling, provider-context-bound OpenRouter admission, fair local generation waiters, short durable capacity continuations that do not consume failure attempts, `failure_attempts`/`product_stage` job fields and migration, capacity-safe provider reconciliation, and live frontend stage labels including retrieval/outline/chapter/export/capacity wait.
- Validation: backend CP10/CP9/CP8/CP3 regression slice passed **148 passed, 2 skipped** after sandbox temp-directory denial; frontend `JobProgress` passed **15 tests** after Vitest sandbox `spawn EPERM`; focused frontend ESLint passed; focused backend Ruff passed; `git diff --check` passed after removing one trailing blank line. Estimated/actual OpenRouter cost for every run **$0.00**.
- Review focus: Redis distributed fairness beyond shared hard capacity is not yet proved; CP15 load and CP16 paid cost/speed gates remain pending. SQLite job execution intentionally falls back to sequential chapter work to avoid unsafe concurrent in-memory test DB access.
- Independent review: **spec FAIL; quality FAIL**. The distributed Redis path only applied the hard cap, retry-scheduled capacity waits hid their `capacity_wait` stage from the API/UI, and capacity continuations reset displayed progress to zero. Reviewer evidence: backend **34 passed**, frontend **15 passed**, plus a deterministic shared-Redis reacquisition probe; estimated/actual provider cost **$0.00**.
- Fix round 1 implementation frozen for independent re-review. Redis now stores only opaque waiter identity/timestamps and atomically alternates queued owner/job admissions across processes; cancellation removes a distributed waiter. Capacity continuations retain progress through scheduling and reclaim; retry-scheduled API responses expose safe `capacity_wait`, and the UI shows the capacity-specific wait message. New deterministic regressions cover the shared-Redis waiting-owner case, capacity-state API response, progress preservation and ordinary-final-attempt fencing.
- Validation after the fix: complete CP10/CP9/CP8/CP3 backend slice **149 passed,2 skipped**; frontend `JobProgress` **16 passed**; scoped backend Ruff and frontend ESLint passed; `git diff --check` passed with only LF-to-CRLF notices. The initial sandbox-only backend run failed before collection because Windows denied pytest's temporary directory; the same suite passed with the approved local test runner. Estimated/actual OpenRouter cost for every run **$0.00**. Re-review package: `.superpowers/sdd/2026-09-05-live-progress-and-capacity/task-5-fix1-review-package.md`.
- Fix round 2: re-review found RPM waits could outlive the 15-second fairness-record TTL, allowing Celery redelivery timing to reorder users/jobs. Fair waiters now live at least 61 seconds and a retry renews the same queued identity without changing its position. The new two-user/consecutive-RPM-window regression initially failed at the second rate-limit window, then passed after identity renewal. Guard suite **29 passed**; combined CP10/CP9/CP8/CP3 slice **151 passed,2 skipped**; scoped Ruff/diff clean with only LF-to-CRLF notices. Estimated/actual provider cost **$0.00**. Independent re-review pending.
- Fix round 2 independent re-review: **spec PASS; quality PASS**. The reviewer confirmed stable identity renewal through consecutive RPM windows, capacity-wait progress/failure-attempt behavior, safe retry-scheduled UI/API stage, and Book checkpoint/resume. Independent offline validation: backend **101 passed**, frontend **16 passed**. CP10 complete; estimated/actual provider cost **$0.00**.

## CP11 — canonical math and speech separation

- Implementation frozen for independent review. Slide and video display fields now use safe NFC/control/line-ending normalization that preserves LaTex, Markdown delimiters, braces, backslashes and Unicode. Only the TTS narration boundary uses a speech-only plain-text normalizer. A shared fixture corpus covers fractions, nested fractions, roots, exponents, matrices, integrals, Vietnamese, mathematical Unicode, CJK and literal question marks; persistence checks assert exact display-text round trips.
- Validation: focused math/text suite **19 passed**. Estimated/actual OpenRouter cost **$0.00**. No pre-edit RED execution is claimed because the implementation was applied before the first focused test run; independent review must validate the direct regressions and all display fields.
- Independent review: **spec PASS; quality PASS**, no P1/P2 findings. Focused offline suite **19 passed**; estimated/actual OpenRouter cost **$0.00**. CP11 complete.

## Balance refresh — 2026-09-05 18:47:57 UTC

- Read-only OpenRouter key lookup: **$9.02170362** remaining from a **$10.00** key limit; cumulative key usage **$0.97829638**. Only numeric balance fields were read, no secret was exposed, no model request ran, and lookup cost **$0.00**.

## CP12 — local rich-text/math renderer

- Implementation frozen for independent review. Added a private local KaTeX/Chromium renderer with bounded input/dimensions/timeout, content-addressed output, raw HTML/image/unsafe macro rejection, blocked remote browser traffic, and explicit render metadata. Focused renderer contract tests **8 passed** at estimated/actual provider cost **$0.00**.
- Current packaging limitation: KaTeX math fonts are local, but no redistributable Vietnamese/CJK text font is in the repository. The renderer fails safely with affected Unicode codepoints instead of substituting a glyph. CP13/CP14 must consume that signal and stop invalid static exports from publication until an approved font is bundled.
- Independent review: **spec FAIL; quality FAIL**. Safe Unicode rejection is correct but does not fulfil Vietnamese/CJK support; output is not job-attempt-owned/claim-fenced; cache does not verify the manifest's font/parser revisions; and a missing Node executable leaks an OS exception. The reviewer confirmed the focused renderer suite **8 passed** and scoped Ruff passed at **$0.00**, but CP12 remains open pending the listed fixes and an approved redistributable font source.

## Evaluation preparation (no paid calls)

- Existing-source inventory found one 174-page Vietnamese PDF and one completed seven-chapter Book. No second/third distinct source-baseline pair exists in inspected live storage. Book model provenance was not persisted; Pro attribution remains unverified.
- Asked for existing local paths/course names asynchronously; implementation continues independently. Three-case promotion remains conditional on real source-based evidence.
- Read-only inventory and public provider documentation checks cost **$0.00**. No document copies, live mutations, or inference calls.

- User could not recall additional examples. Located and linked the existing source and Book; continuing offline synthetic fidelity fixtures. Additional real baseline comparisons remain unverified, with no paid baseline generation authorized.
- Browser tooling preparation: read the installed browser skill and searched available/deferred tools; its required browser execution tool is unavailable in this session. Visual validation uses the repository Playwright runner and local screenshot artifacts. No interactive browser result is claimed from the unavailable connection.

## Balance refresh — 2026-09-05 09:00:39 UTC / 16:00:39 Vietnam

- Read-only /key and /credits: key allowance remaining **$9.02170362**, cumulative key usage **$0.97829638**; account wallet remaining **$520.088690501**. Unchanged from starting snapshot. Lookup estimated/actual cost **$0.00**; no secret exposed.

- Offline baseline sizing: persisted Book compact JSON measures 24,358 tokens with the pinned Google tokenizer. At candidate output pricing this illustrates $0.060895 for output alone; it is not historical billing or full-Book cost. Measurement itself cost $0 and is recorded with limitations in the source inventory. Keep allocation adaptive to required source coverage.

- Source evaluation preparation now includes an AI-proposed manifest: 8 source objectives,18 factual probes,12 equation/table probes with page references. No inference/network calls or tests; OpenRouter cost $0. These are proposed automated checks, not a completed human rubric or proof of exhaustive coverage.
- Source visual verification: all 12 proposed equation/table probes were checked against 16 locally rendered source pages. Corrected one probe that had combined separately presented hard-margin and soft-margin SVM expressions. AI-only inspection, no human rubric approval; cost $0.

## Balance refresh — 2026-09-05 09:32:42 UTC / 16:32:42 Vietnam

- Read-only /key and /credits: key allowance remaining **$9.02170362**, cumulative key usage **$0.97829638**; account wallet remaining **$520.088690501**. Unchanged from starting snapshot. Lookup estimated/actual cost **$0.00**; no secret exposed.

## Balance refresh after CP3 — 2026-09-05 10:04:20 UTC / 17:04:20 Vietnam

- Read-only /key and /credits: key allowance remaining **$9.02170362**, cumulative key usage **$0.97829638**; account wallet remaining **$520.088690501**. Unchanged from starting snapshot. Lookup and every CP3 test estimated/actual provider cost **$0.00**; no secret exposed.
